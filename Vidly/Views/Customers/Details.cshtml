@model Vidly.Models.Customer
@{
    ViewBag.Title = "Details";
}

<h2>@Model.Name</h2>

<ul>
    <li>Membership Type: @Model.MembershipType.Name</li>
    <li>Birthdate:
    @if (Model.Birthdate.HasValue)
    {
        <text>@Model.Birthdate.Value.ToLongDateString()</text>
    }
    </li>
</ul>

